# Ignore project directories with content
info/
input/
output/
logs/
vector_db/
.gemini/
.venv/
__pycache__/

# Ignore specific files
poetry.lock
test*.py
ragagentOrig.py
.DS_Store
*.pyc

# Keep the directories themselves (if empty, git won't track them, so we can add .gitkeep if needed, 
# but user said "without content", implying the dirs might be local only. 
# Git doesn't track empty dirs. I'll stick to ignoring their content.)
